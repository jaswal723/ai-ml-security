# 🐍 Phase 0 — The “Pre-Week” Python Refresher

**AI/ML Security Architect: 12-Month Transformation Roadmap**

---

## 🎯 Goal

Before diving into data pipelines and machine learning, this phase ensures you have a **solid foundation in Python** — focusing on how it stores, manipulates, and evaluates **security data in memory**.

By the end of this phase, you’ll be confident writing concise, Pythonic scripts to process logs, analyze threat data, and automate repetitive SOC workflows.

---

## 🧩 Learning Objectives

✅ Master Python’s **core data structures** (lists, dictionaries, sets)
✅ Manipulate and format strings to extract intelligence from logs
✅ Write **functions and list comprehensions** to perform logic efficiently
✅ Develop **Python thinking** — readable, efficient, and secure code

---

## 🧠 Topics Covered

### 1️⃣ Core Data Structures

**Concepts:** `list()`, `.append()`, `.pop()`, `dict()`, `.keys()`, `.values()`, `.items()`, `set()`, `.add()`

### 2️⃣ String Manipulation & Formatting

**Concepts:** `.split()`, `.strip()`, `.replace()`, `.lower()`, `.upper()`, `f-strings`

### 3️⃣ Functions & Pythonic Logic

**Concepts:** `def`, `return`, default arguments, list comprehensions (`[x for x in list if condition]`)

---